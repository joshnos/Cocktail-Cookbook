import 'package:flutter/material.dart';

class AlcoholicScreen extends StatelessWidget {
  const AlcoholicScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Alcoholic');
  }
}
